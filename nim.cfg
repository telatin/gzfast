# Default local build profile for gzfast.
#
# Keep this portable: do not add architecture-specific flags such as
# -march=native here. Use those only in explicit local benchmarks.
--mm:orc
--threads:on
-d:release
--opt:speed
